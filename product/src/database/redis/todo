리팩터링
- 쿼리 객체를 만들기까지만하고 실행하는 인터페이스 노출
- 엔티티와 레디스캐시 스키마를 전반적으로 리팩터링해야함.
  - 현재, 엔티티를 정의할때 생성자함수를 정의해야하며, 엔티티의 프로토타입이 확장되면서 레포지토리에서 나온 캐시들은 엔티티의 인스턴스가됨.
  - 엔티티는 데코레이터 써서 최소한으로 정의하도록 하고, 스키마서비스에서 레디스캐시클래스를 만들어서 팩토리로 내보내고, 레포지토리에서 나온 캐시들이 레디스캐시클래스의 인스턴스가 되도록 해보자.

SchemaService unit test