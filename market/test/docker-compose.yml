version: "3"

services:
  redis:
    container_name: pfa-market-test-mongo
    image: mongo:latest
    ports:
      - "27017:27017"
    expose:
      - "27017"
    restart: always
    profiles:
      - test